package org.example;
public class Main {
    /*
    if (condición) {
    // Código a ejecutar si la condición es verdadera
} else {
    // Código a ejecutar si la condición es falsa
}
     */
    public static void main(String[] args) {
        // operandos logicos==, = >=, > ,
        // || (or), && (and), ! (not) negar el resultado o cambiarlo
        int var = 6;
        int var2 = 10;

        if (((var == 6) && !( var2 >10 ))) // CONDICION DEBE DECIR SI EL FALSO O VERDADERO - doble igual "==" realizar una operación de
            // comparacion
        {
           System.out.println("La comparacion de los valores " +var + " y " + var2 + " es verdadera");
            //CODIGO DENTRO DE LAS LLAVES

        }
        else if ()
        {
            System.out.println("La operacion es falsa");
            //CODIGO DENTRO DE LAS LLAVES
        } else if (){

        }
    }
}